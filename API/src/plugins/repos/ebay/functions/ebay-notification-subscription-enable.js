const { utils } = require('./utils');

module.exports = {
  async ebay_notification_subscription_enable(node, msg, inputs, opts) {
    const log = (opts && opts.log) ? opts.log : () => {};
    const d = inputs || {};
    let reqPath = "https://api.ebay.com/commerce/notification/v1/subscription/{subscriptionId}/enable";
    const subscriptionid = String(d.subscriptionid || '').trim();
    if (!subscriptionid) return { ok: false, error: 'subscriptionid requis.' };
    reqPath = reqPath.replace('{subscriptionid}', encodeURIComponent(subscriptionid));

    const query = {};
    

    let body = undefined;
    if (d.body !== undefined && d.body !== null && d.body !== '') {
      if (typeof d.body === 'object') body = d.body;
      else {
        try { body = JSON.parse(String(d.body)); } catch { return { ok: false, error: 'JSON invalide dans body.' }; }
      }
    }

    log('Requête en cours...');
    const res = await utils.providerRequest(opts, reqPath, { method: 'POST', query, body });
    if (!res.ok) return { ok: false, error: res.error, status: res.status, details: res.details };

    return {
      ok: true,
      status: res.status,
      message: 'Action exécutée.',
      raw: res.data || null
    };
  }
};
