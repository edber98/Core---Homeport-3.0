apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-kinn-homeport
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-kinn-homeport
  ports:
    - port: {{ .Values.kinn.kinnHomeport.port | int }}
      targetPort: {{ .Values.kinn.kinnHomeport.port | int }}
