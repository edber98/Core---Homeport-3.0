# Prod : 3 replicas API, rolling strict (maxUnavailable=0)
- op: replace
  path: /spec/replicas
  value: 3
- op: replace
  path: /spec/strategy
  value:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
